export const metadata = {
  title: "AI Music Trend Challenge | SQL Adventure",
  description:
    "Stellar Sound TikTok channel challenge: download a music trends CSV, use AI with clear prompts, four steps, then a launch graphic.",
};

export default function AiMusicTrendChallengeLayout({ children }) {
  return children;
}
